<h1>Projects#index</h1>

<% @projects.each do |project| %>
  <p><%= image_tag "#{project.image_url}" %></p>
  <h2><%= "#{project.title}" %></h2>
  <h3>Description</h3>
  <p><%= "#{project.description}" %></p>
<% end %>
