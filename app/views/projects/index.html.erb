<h1>Projects#index</h1>

<% @projects.each do |project| %>
  <p><%= link_to image_tag("#{project.image_url}"), url_for(project) %></p>
  <h2><%= "#{project.title}" %></h2>
  <h3>Description</h3>
  <p><%= "#{project.description}" %></p>
<% end %>
